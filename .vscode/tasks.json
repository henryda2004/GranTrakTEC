{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compilar y Ejecutar en QEMU",
            "type": "shell",
            "command": "nasm -f bin boot1.asm -o boot1.bin && nasm -f bin stage.asm -o stage.bin && cat boot1.bin stage.bin > disk.img && qemu-system-i386 -drive format=raw,file=disk.img,if=floppy -boot a",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        }
    ]
}